The ImageUtil file lets you define all the Image related info. It defines the Constrained capture of the tool and also use iTextSharp to export the file as PDF. 

The ImageUtil is the heart of the tool. 